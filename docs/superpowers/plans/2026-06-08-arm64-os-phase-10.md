# Phase 10 Plan (shell) -- executing-plans

Task 1 (TEST FIRST): SYS_READDIR. Add `test_syscall_readdir` (open-less: do_syscall
SYS_READDIR on "/" returns names, -1 at end). RED (undefined). Implement
SYS_READDIR in syscall.c (resolve dir via vfs_lookup, vfs_readdir). GREEN. Commit.

Task 2: keyboard input. uart_getc() (poll PL011 RX). SYS_READ fd 0 -> uart_getc
with yield-poll. Commit.

Task 3: the shell. user/sh.c (new /bin/init) with builtins help/echo/ls/cat/exit;
ulib sys_readdir/sys_read line helpers. Makefile: build sh.c instead of prog.c.
Feed stdin, observe. make test green. Commit.

Task 4: notes. Commit.

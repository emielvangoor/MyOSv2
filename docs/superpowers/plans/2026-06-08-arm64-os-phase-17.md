# MyOSv2 Phase 17 — pipes Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans. Checkbox steps.

**Goal:** Pipes (blocking ring-buffer, two ends), `dup2`, file refcounting, and a `hello | wc` shell pipeline.

Reference: `docs/superpowers/specs/2026-06-08-arm64-os-phase-17-design.md`.

---

### Task 1: `struct pipe` + ring buffer + EOF/broken (pipe.c)

**Files:** `src/pipe.h`, `src/pipe.c`, `src/vfs.h` (file fields), `src/tests.c`

- [ ] **Step 1 — extend `struct file`** in `vfs.h`: add `struct pipe *pipe; int writable; int ref;` (forward-declare `struct pipe;`).

- [ ] **Step 2 — failing tests** (new section in tests.c, `#include "pipe.h"`):
```c
static void test_pipe_write_then_read(void)
{
    pmm_init(); kheap_init();
    struct pipe *p = pipe_alloc();
    struct file wf = { .pipe = p, .writable = 1, .ref = 1 };
    struct file rf = { .pipe = p, .writable = 0, .ref = 1 };
    KASSERT(pipe_write(&wf, "hello", 5) == 5);
    char b[8] = {0};
    KASSERT(pipe_read(&rf, b, 8) == 5);
    KASSERT(b[0]=='h' && b[4]=='o');
    KASSERT(p->count == 0);
}
static void test_pipe_eof(void)
{
    pmm_init(); kheap_init();
    struct pipe *p = pipe_alloc();
    p->writers = 0;                       // no writers, empty
    struct file rf = { .pipe = p, .writable = 0, .ref = 1 };
    char b[4];
    KASSERT(pipe_read(&rf, b, 4) == 0);   // EOF
}
static void test_pipe_broken(void)
{
    pmm_init(); kheap_init();
    struct pipe *p = pipe_alloc();
    p->readers = 0;                       // no readers
    struct file wf = { .pipe = p, .writable = 1, .ref = 1 };
    char big[PIPE_SIZE];
    for (int i=0;i<PIPE_SIZE;i++) big[i]='x';
    p->count = PIPE_SIZE; p->w = 0; p->r = 0;   // full
    KASSERT(pipe_write(&wf, big, PIPE_SIZE) == -1);  // broken pipe
}
static void test_pipe_wraps(void)
{
    pmm_init(); kheap_init();
    struct pipe *p = pipe_alloc();
    struct file wf = { .pipe = p, .writable = 1, .ref = 1 };
    struct file rf = { .pipe = p, .writable = 0, .ref = 1 };
    char buf[100]; for (int i=0;i<100;i++) buf[i]=(char)i;
    // advance the ring near the end, then write across the wrap
    p->r = p->w = PIPE_SIZE - 10; 
    KASSERT(pipe_write(&wf, buf, 50) == 50);
    char out[50] = {0};
    KASSERT(pipe_read(&rf, out, 50) == 50);
    for (int i=0;i<50;i++) KASSERT(out[i]==(char)i);
}
```
Registry: four entries.

- [ ] **Step 3 — RED.**

- [ ] **Step 4 — `pipe.h`** (`#define PIPE_SIZE 4096`, `struct pipe {...}`,
`struct file; struct pipe *pipe_alloc(void); int pipe_read(struct file*, void*, uint64_t);
int pipe_write(struct file*, const void*, uint64_t); void pipe_close(struct file*);`)
**and `pipe.c`:**
```c
struct pipe *pipe_alloc(void)
{
    struct pipe *p = kmalloc(sizeof(struct pipe));
    p->r = p->w = p->count = 0; p->readers = 1; p->writers = 1;
    return p;
}
int pipe_read(struct file *f, void *buf, uint64_t len)
{
    struct pipe *p = f->pipe;
    while (p->count == 0 && p->writers > 0) { yield(); }
    if (p->count == 0) { return 0; }                 // EOF
    uint64_t n = 0; char *b = buf;
    while (n < len && p->count > 0) { b[n++] = p->buf[p->r]; p->r = (p->r+1) % PIPE_SIZE; p->count--; }
    return (int)n;
}
int pipe_write(struct file *f, const void *buf, uint64_t len)
{
    struct pipe *p = f->pipe;
    const char *b = buf; uint64_t n = 0;
    while (n < len) {
        while (p->count == PIPE_SIZE && p->readers > 0) { yield(); }
        if (p->readers == 0) { return n ? (int)n : -1; }   // broken pipe
        while (n < len && p->count < PIPE_SIZE) { p->buf[p->w] = b[n++]; p->w = (p->w+1) % PIPE_SIZE; p->count++; }
    }
    return (int)n;
}
void pipe_close(struct file *f)
{
    struct pipe *p = f->pipe;
    if (f->writable) { p->writers--; } else { p->readers--; }
    if (p->readers == 0 && p->writers == 0) { kfree(p); }
}
```
(`pipe.c` includes `kheap.h`, `sched.h` for yield, `vfs.h` for struct file, `pipe.h`.)

- [ ] **Step 5 — GREEN. Step 6 — commit + push** `feat(ipc): pipe ring buffer + EOF/broken (Phase 17, test-first)`.

---

### Task 2: VFS routing + file refcounting + fork incref

**Files:** `src/vfs.c`, `src/sched.c`, `src/tests.c`

- [ ] **Step 1 — failing test:**
```c
static void test_file_refcount(void)
{
    pmm_init(); kheap_init();
    vfs_mount_root(ramfs_type());
    vfs_create("/f", VN_FILE);
    struct file *f = vfs_open("/f");        // ref = 1
    KASSERT(f->ref == 1);
    file_dup(f);                            // ref = 2
    KASSERT(f->ref == 2);
    vfs_close(f);                           // ref = 1, NOT freed
    KASSERT(f->ref == 1);
    vfs_write(f, "x", 1);                   // still usable
    vfs_close(f);                           // ref = 0, freed (no crash)
}
```
Registry entry.

- [ ] **Step 2 — RED** (`file_dup`/`f->ref` missing).

- [ ] **Step 3 — implement.** `vfs.c`:
- `vfs_open`: set `f->pipe = 0; f->writable = 0; f->ref = 1;`.
- `vfs_read`/`vfs_write`: `if (f->pipe) return pipe_read/pipe_write(f, buf, len);` first.
- `vfs_close`: `if (--f->ref > 0) return; if (f->pipe) pipe_close(f); kfree(f);`.
- add `struct file *file_dup(struct file *f) { f->ref++; return f; }` (declare in vfs.h).
`sched.c` `sched_fork`: after copying `t->fds[i] = current->fds[i]`, `if (t->fds[i]) file_dup(t->fds[i]);`.

- [ ] **Step 4 — GREEN. Step 5 — commit + push** `feat(ipc): file refcounting + pipe routing (Phase 17, test-first)`.

---

### Task 3: SYS_PIPE + SYS_DUP2 + fd-first routing

**Files:** `src/syscall.h`/`syscall.c`, `user/syscalls.h`, `user/ulib.*`

- [ ] **Step 1 — syscall numbers** `SYS_PIPE 18`, `SYS_DUP2 19` (both headers).

- [ ] **Step 2 — fd-first routing in `syscall.c`** for `SYS_WRITE`/`SYS_READ`:
prefer `fds[fd]` when set, else console for fd 0/1/2. Add cases:
```c
case SYS_PIPE: {                          // x0 = int fd[2]
    int *ufd = (int *)(uintptr_t)tf->x[0];
    struct file **fds = sched_current_fds();
    struct pipe *p = pipe_alloc();
    struct file *rf = kmalloc(sizeof(struct file)); *rf = (struct file){0}; rf->pipe=p; rf->writable=0; rf->ref=1;
    struct file *wf = kmalloc(sizeof(struct file)); *wf = (struct file){0}; wf->pipe=p; wf->writable=1; wf->ref=1;
    int r=-1,w=-1;
    for (int i=0;i<16 && r<0;i++) if(!fds[i]){fds[i]=rf;r=i;}
    for (int i=0;i<16 && w<0;i++) if(!fds[i]){fds[i]=wf;w=i;}
    if (r<0||w<0){ ret=-1; break; }
    ufd[0]=r; ufd[1]=w; ret=0; break;
}
case SYS_DUP2: {                          // x0 = old, x1 = new
    struct file **fds = sched_current_fds();
    uint64_t o=tf->x[0], n=tf->x[1];
    if (o>=16 || n>=16 || !fds[o]) { ret=-1; break; }
    if (fds[n]) { vfs_close(fds[n]); }
    fds[n] = file_dup(fds[o]); ret=(long)n; break;
}
```
(include `pipe.h`, `kheap.h`.)

- [ ] **Step 3 — ulib:** `int pipe(int fd[2]); int dup2(int old, int newfd);`.

- [ ] **Step 4 — build + gate green. Commit + push** `feat(ipc): SYS_PIPE + SYS_DUP2 + fd-first stdio routing (Phase 17)`.

---

### Task 4: `/bin/wc` + shell `A | B`

**Files:** `user/wc.c`, `user/sh.c`, `Makefile`, `src/initrd.c`

- [ ] **Step 1 — `user/wc.c`:** read stdin to EOF, print byte count.
```c
#include "ulib.h"
int umain(void)
{
    char buf[256]; long total = 0, n;
    while ((n = sys_read(0, buf, sizeof(buf))) > 0) { total += n; }
    // print total then newline
    char b[24]; int i=0; long v=total;
    if(v==0) b[i++]='0'; while(v>0){b[i++]=(char)('0'+v%10); v/=10;}
    while(i>0) sys_write(1,&b[--i],1);
    sys_write(1,"\n",1);
    return 0;
}
```

- [ ] **Step 2 — shell:** detect a `|`, split into two commands, and run the
pipeline (pipe + two forks with dup2 + close + wait). Add a `run_pipeline(left,right)`
in sh.c; `run_external` path checks for `|`.

- [ ] **Step 3 — build + live:** add `wc` to `PROGS` + initrd. `make run` (piped):
`hello | wc` -> `22`. Confirm plain commands + the shell still work.

- [ ] **Step 4 — commit + push** `feat(ipc): /bin/wc + shell pipelines (Phase 17)`.

---

### Task 5: Notes + README

- [ ] `docs/notes/phase-17.md` + README line (pipes/pipelines). Commit + push.

## Self-Review
- Coverage: pipe ring ✓(T1), refcount/routing ✓(T2), syscalls ✓(T3), live pipeline ✓(T4), notes ✓(T5).
- Types: `struct file` gains `pipe/writable/ref`; `file_dup` in vfs.h; `pipe_*` in pipe.h; fork increfs fds.
